PHASE KT-1.3 — HIERARCHICAL / COLLAPSIBLE KNOWLEDGE TREE

STRICT SCOPE:
Build the hierarchical student Knowledge Tree UI using the REAL existing
curriculum hierarchy.

Do NOT implement KT-1.4 mastery roll-up yet.
Do NOT implement KT-1.5 Detail Panel yet.
Do NOT change mastery formulas.
Do NOT change evidence scoring.
Do NOT change mapping.
Do NOT change quizzes.
Do NOT change AI Teacher.
Do NOT introduce a new DB hierarchy.

==================================================
0. CANONICAL MODEL — LOCKED
==================================================

The canonical technical hierarchy is:

Subject
  → Lesson
    → Topic / Knowledge Group
      → MicroNode / Atomic Knowledge Unit

Student-facing terminology:

Առարկա
  → Դաս
    → Գիտելիքի խումբ
      → Գիտելիքի հանգույց

IMPORTANT:

There is NO real Section/Chapter entity in the current DB.

There is NO Node → Subnode hierarchy.

lesson_nodes are already the atomic MicroNodes.

Do NOT invent a parent_id or fake Node/Subnode layer.

==================================================
1. AUDIT FIRST
==================================================

Before changing code, verify the current KT-1.2 implementation.

Inspect:

- artifacts/myaiteacher/src/pages/knowledge-tree.tsx
- artifacts/api-server/src/routes/knowledge-tree.ts
- relevant generated API types/hooks
- lesson_topics
- lessons
- lesson_nodes
- knowledge_nodes

Report the CURRENT response shape of:

GET /api/knowledge-tree/:subjectId

and confirm whether it is still a flat atomic-node array.

Also verify:

- subjectId
- lessonId
- lessonTitle
- topicId
- topicTitle
- lessonNodeId
- nodeTitle
- sequence
- masteryScore
- confidenceScore
- masteryLevel

Which of these are already returned?
Which require additional JOINs?

Do NOT implement until this is clear.

==================================================
2. NEW KT RESPONSE SHAPE
==================================================

Restructure the subject Knowledge Tree response into the real hierarchy.

Target shape conceptually:

{
  subjectId,
  subjectName,

  lessons: [
    {
      lessonId,
      lessonTitle,
      lessonStatus,
      sequence/order if available,

      topics: [
        {
          topicId,
          topicTitle,
          sequence,

          nodes: [
            {
              lessonNodeId,
              title,
              sequence,

              masteryScore,
              confidenceScore,
              masteryLevel,

              // learner state may be synthesized
              // if no knowledge_nodes row exists
            }
          ]
        }
      ],

      ungroupedNodes: [
        ...
      ]
    }
  ]
}

IMPORTANT:

Nodes with topic_id = null must NOT disappear.

Represent them cleanly as either:

- "Առանց խմբի"
or
- an explicit ungroupedNodes collection

Choose the cleaner implementation.

Do not create fake DB topics.

==================================================
3. ORDERING
==================================================

All hierarchy levels must render deterministically.

Lesson:
use the actual authoritative lesson ordering available in current schema/API.

Topic:
lesson_topics.sequence

MicroNode:
lesson_nodes.sequence

If Lesson has no sequence field:
report the actual authoritative ordering source and use the smallest
deterministic fallback.

Do NOT sort nodes by DB id unless no proper ordering source exists.

If sequence duplicates exist in legacy data:
do NOT silently mutate real data.

Use a deterministic fallback for rendering and report the integrity issue.

==================================================
4. VISIBILITY CONTRACT — PRESERVE KT-1.2
==================================================

Do NOT regress the visibility contract.

Student-visible knowledge units must still come only from legitimate
student-visible curriculum:

lesson.status = active
AND
lesson_node.status = approved
AND
subject belongs to student's enrolled curriculum.

No knowledge_nodes row is required.

If knowledge_nodes is absent:

state = not_started
UI = "Դեռ չի ուսումնասիրել"

Do NOT insert fake learner-state rows.

==================================================
5. NEW SUBJECT KNOWLEDGE TREE UX
==================================================

Replace the current flat card grid with a hierarchical collapsible view.

TOP:

[← Հետ]
Ֆիզիկա
Գիտելիքի ծառ

Optional top-level summary area may remain minimal in KT-1.3.

Do NOT add aggregate mastery % yet — KT-1.4 owns that.

==================================================
6. LESSON LEVEL
==================================================

Each Lesson is a collapsible container.

Example:

▶ Դաս 1 — Ֆիզիկայի ուսումնասիրման առարկան

When collapsed:
show only compact lesson header.

When expanded:
show its Knowledge Groups / Topics.

DEFAULT:

Lessons should be collapsed by default for scalability.

But ONE sensible lesson may auto-expand, for example:
- current/active learning lesson if such context already exists reliably
OR
- first lesson

Do NOT invent orchestration logic.

If there is no reliable "current lesson" signal:
expand the first lesson only.

==================================================
7. KNOWLEDGE GROUP / TOPIC LEVEL
==================================================

Inside an expanded Lesson:

▶ Բնության երևույթների և ֆիզիկայի հիմունքներ

When opened:
show the MicroNodes belonging to this Topic.

Topic is a grouping container only.

Do NOT treat Topic as an atomic mastery unit yet.

KT-1.4 will add roll-up values later.

==================================================
8. MICRONODE / KNOWLEDGE UNIT LEVEL
==================================================

Each MicroNode is the leaf.

Show at minimum:

- title
- 4-state badge
- node masteryScore %
- optional confidence if already useful and not visually noisy

Required four visible states:

Գիտի
Մասնակի գիտի
Չգիտի
Դեռ չի ուսումնասիրել

Use the SAME existing authoritative classification logic.

Do NOT create new thresholds.

Do NOT implement Bloom/depth yet.

Do NOT implement difficulty yet.

Do NOT implement next-best-action engine yet.

==================================================
9. STATE VISUALIZATION
==================================================

Use a consistent visual system.

Each MicroNode should clearly communicate state through:

- badge
- icon or subtle indicator
- label

Do not rely only on color.

Example:

✓ Գիտի
◐ Մասնակի գիտի
! Չգիտի
○ Դեռ չի ուսումնասիրել

Use Armenian labels already established in the product.

Do NOT redesign the meaning of states.

==================================================
10. COLLAPSIBLE BEHAVIOR
==================================================

Required:

Subject page
→ Lessons collapsible

Expanded Lesson
→ Topics collapsible

Expanded Topic
→ MicroNodes visible

The user should never be forced to render the whole year's nodes at once.

Support:

- open one lesson
- open multiple lessons if desired
- open one/multiple topic groups

Keep UI state local.

Do not persist expansion state to DB.

==================================================
11. SCALABILITY
==================================================

KT-1.1 estimated a full-year subject could have ~2,000+ atomic nodes.

Therefore:

DO NOT render all MicroNodes for all lessons eagerly if avoidable.

Preferred architecture:

- load subject/lesson hierarchy initially
- render MicroNodes only for expanded lesson/topic branches

If current API returns all nodes at once and changing to lazy per-lesson
loading would significantly enlarge scope:

you may keep one hierarchical API response for KT-1.3,
BUT the frontend must at minimum avoid mounting all closed-branch node cards
into the DOM.

Report the tradeoff.

Do not add pagination unless genuinely needed now.

==================================================
12. FILTERS
==================================================

Preserve the four-state filters:

Բոլորը
Գիտի
Մասնակի գիտի
Չգիտի
Դեռ չի ուսումնասիրել

Filtering must work WITHIN the hierarchy.

Rules:

- filter applies to MicroNodes
- Lesson/Topic containers with zero matching descendants may be hidden
  or shown disabled; choose one consistent UX
- "Բոլորը" restores full hierarchy

Default filter:
Բոլորը

Changing subject:
reset to Բոլորը

Do not let stale filter state carry across subjects.

==================================================
13. SEARCH — OPTIONAL ONLY IF LOW COST
==================================================

If the current architecture makes it trivial, add a simple text search
across MicroNode titles.

But this is OPTIONAL for KT-1.3.

Do not delay the phase or create a backend search system.

If not implemented, report as deferred.

==================================================
14. BACK NAVIGATION
==================================================

Keep current canonical routing:

Sidebar
→ /kt-subjects
→ Subject
→ /knowledge-tree/:subjectId

Back from subject KT:

/knowledge-tree/:subjectId
→ /kt-subjects

Do not reintroduce legacy KT entry behavior.

==================================================
15. DO NOT ADD MASTERY ROLL-UP YET
==================================================

Very important:

Do NOT calculate:

Subject mastery %
Lesson mastery %
Topic mastery %

KT-1.4 will define the authoritative roll-up formula.

For KT-1.3:

MicroNode masteryScore may display because it already exists.

Parent levels may show:

"Յուրացում՝ —"

or no percentage.

Do NOT silently use simple averages.

==================================================
16. DO NOT ADD DETAIL PANEL YET
==================================================

MicroNode click may be prepared for future detail opening,
but do NOT implement the full Detail Panel yet.

KT-1.5 owns:

- learning objective
- evidence
- confidence detail
- source
- history

For now MicroNode is a compact leaf card/row.

==================================================
17. REQUIRED REAL PHYSICS UAT
==================================================

Use the current real Physics subject read-only for manual verification.

Current legitimate Physics atomic units include:

- Ատոմներ
- Տարրեր
- Մոլեկուլներ

Expected result:

Ֆիզիկա
  → [Lesson]
      → [Knowledge Group]
          → Ատոմներ
          → Տարրեր
          → Մոլեկուլներ

Use the ACTUAL lesson/topic grouping from DB.

Do NOT force these 3 under a fake group if DB grouping differs.

==================================================
18. ACCEPTANCE TESTS
==================================================

HIERARCHY

T01 Subject response contains Lessons.

T02 Lesson contains real Topic groups.

T03 Topic contains correct MicroNodes.

T04 MicroNode IDs match canonical lesson_nodes.

T05 No fake Node/Subnode level introduced.

T06 topic_id=null nodes are preserved.

ORDERING

T07 Topics ordered by topic.sequence.

T08 MicroNodes ordered by node.sequence.

T09 deterministic fallback exists for legacy sequence ties.

VISIBILITY

T10 draft lessons excluded.

T11 draft/unapproved MicroNodes excluded.

T12 no-KN MicroNode appears as not_started.

T13 no fake KN row created.

FOUR STATES

T14 mastered maps correctly.

T15 weak maps correctly.

T16 in_progress maps correctly.

T17 not_started maps correctly.

FILTERING

T18 "Բոլորը" shows full hierarchy.

T19 each state filter shows only matching MicroNodes.

T20 empty hierarchy branches behave consistently.

NAVIGATION

T21 /kt-subjects → subject KT still works.

T22 back → /kt-subjects.

SCALABILITY

T23 collapsed lessons do not mount all descendant MicroNode cards.

DATA INTEGRITY

T24 no duplicate lessonNodeIds.

T25 no test pollution.

==================================================
19. MANUAL UAT CHECKLIST
==================================================

After implementation give me exact browser steps.

I should test:

1. Student login.
2. Sidebar → Գիտելիքի ծառ.
3. Select Physics.
4. See subject page with Lesson containers.
5. Expand one Lesson.
6. See its Knowledge Groups.
7. Expand a Knowledge Group.
8. See its MicroNodes.
9. Verify the 3 known Physics units appear under their REAL DB hierarchy.
10. Collapse the Lesson.
11. MicroNodes disappear from DOM/view.
12. Open another Lesson.
13. Test each of the 4 state filters.
14. Return to "Բոլորը".
15. Navigate back to subject picker.
16. Re-enter Physics.
17. Page starts in deterministic default state.

==================================================
20. FINAL REPORT
==================================================

Return:

A. PRE-IMPLEMENTATION RESPONSE SHAPE

B. FINAL CANONICAL HIERARCHY USED

C. API RESPONSE SHAPE AFTER KT-1.3

D. FILES CHANGED

E. ORDERING RULES

F. VISIBILITY RULES

G. COLLAPSIBLE UX

H. FILTER BEHAVIOR

I. PHYSICS REAL UAT
Show exact:
Lesson
→ Topic
→ MicroNode titles

J. SCALE / DOM BEHAVIOR

K. TEST RESULTS T01–T25

L. MANUAL UAT STEPS

M. DEFERRED ITEMS
- mastery roll-up → KT-1.4
- Detail Panel → KT-1.5
- Bloom/depth/difficulty → later personalization Phase 2
- spaced repetition → later

FINAL VERDICT:

KT-1.3 = PASS — READY FOR KT-1.4

or

KT-1.3 = FAIL — DO NOT PROCEED

STOP after KT-1.3.
Do NOT automatically begin KT-1.4.