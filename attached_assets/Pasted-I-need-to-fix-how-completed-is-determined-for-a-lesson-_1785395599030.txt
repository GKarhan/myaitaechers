I need to fix how "completed" is determined for a lesson on the student side. Right now several places read the lesson's own global `status` column (draft/active/completed) to decide whether to show a lesson as completed — but that column is shared across ALL students taking that lesson, so if one student finishes it, every other student enrolled in the same lesson would incorrectly show it as completed too. Completion must be per-student, sourced from `lessonSessionsTable.status` (which already has its own "active"/"completed" value per user+lesson), not from the lesson's own global status.

MANDATORY ARMENIAN SCRIPT RULE: Any Armenian text you write or touch MUST be real Armenian Unicode script (Ա-Ֆ, ա-ֆ). Never transliterate Armenian into Latin letters or Cyrillic. (Some existing text in these files is pre-existing transliterated text — that's a separate known issue, don't fix it here, just don't add any new transliterated text yourself.)

BACKEND CHANGE — artifacts/api-server/src/routes/student.ts, GET /student/course-lessons
This endpoint currently returns each lesson's raw `status` column (draft/active/completed — filtered server-side to exclude drafts). Add a per-student completion signal: left-join `lessonSessionsTable` filtered to `lessonSessionsTable.userId = req.userId` for each returned lesson, and include a new field in the response, e.g. `mySessionStatus: string | null` (the value of that student's own lessonSessionsTable.status for this lesson — "active", "completed", or null if they've never started a session for it). Keep the existing `status` field (the lesson's own draft/active/completed column) unchanged in the response — just add this new field alongside it.

FRONTEND CHANGE — artifacts/myaiteacher/src/pages/subject-detail.tsx and dashboard.tsx
Wherever a lesson's completion state is currently decided by checking `lesson.status === "completed"` (the status badge logic, the Learn-button gating from the last fix, the completed-count stats, and anywhere else status is used to mean "this student finished it"), change it to check `mySessionStatus === "completed"` instead. The lesson's own `status` field should now only be used to mean "has the teacher assigned it" (i.e. `status === "active"` still gates whether the Learn button/assignment shows at all — keep that part), while whether it's marked as done for THIS student comes from `mySessionStatus`. Concretely:
- A lesson is "completed" (for badge/count purposes) when `mySessionStatus === "completed"`, regardless of the lesson's own `status` value.
- A lesson shows the Learn button (from the last fix) when the lesson's own `status === "active"` AND `mySessionStatus !== "completed"` (don't show Learn on something this student already finished — show a completed badge instead in that case).
- Update any completed-lesson counters (e.g. "Ավարտված X/Y" stats) to count by `mySessionStatus === "completed"` per lesson, not by the lesson's own status field.

Do this consistently in both files, matching whatever local variable names they already use for lesson status handling. Do not change anything else. Run a typecheck and confirm zero errors, then describe how the numbers/badges would look for a student who has completed one lesson while classmates haven't touched it (should NOT show completed for the classmates).