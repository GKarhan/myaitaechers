ԽՆԴԻՐ. Ներկայումս, երբ admin-ը ստեղծում է դասարան և կցում ուսուցիչ, ուսուցչի
"Դասարաններ" բաժնում ցուցադրվում են ՆՐԱ ԲՈԼՈՐ որակավորության առարկաները
(teacher.subjects), անկախ նրանից՝ ինքը իրապես դասավանդում է դրանք հենց այս
կոնկրետ դասարանում, թե ոչ (հաստատված՝ teacher-dashboard.tsx-ի classSubjects
հաշվարկը, երբ schedule-ը դատարկ է, ընկնում է ամբողջ teacherProfile.subjects-ի
վրա)։ Պետք է ավելացնել հստակ, պահվող «այս ուսուցիչը այս դասարանում
դասավանդում է հենց այս կոնկրետ առարկան(ները)» assignment։

═══════════════════════════════════════════
ՄԱՍ 1 — Նոր schema. teacher_class_subjects junction table
═══════════════════════════════════════════

Ստեղծիր lib/db/src/schema/teacher-class-subjects.ts.

import { pgTable, serial, integer, timestamp, unique } from "drizzle-orm/pg-core";
import { classesTable } from "./classes";
import { subjectsTable } from "./subjects";
import { teachersTable } from "./teachers";

export const teacherClassSubjectsTable = pgTable("teacher_class_subjects", {
  id: serial("id").primaryKey(),
  classId: integer("class_id").notNull().references(() => classesTable.id, { onDelete: "cascade" }),
  teacherId: integer("teacher_id").notNull().references(() => teachersTable.id, { onDelete: "cascade" }),
  subjectId: integer("subject_id").notNull().references(() => subjectsTable.id, { onDelete: "cascade" }),
  createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(),
}, (t) => ({
  uniqueAssignment: unique().on(t.classId, t.teacherId, t.subjectId),
}));
export type TeacherClassSubject = typeof teacherClassSubjectsTable.$inferSelect;

Ավելացրու export-ը schema/index.ts-ում։ Գործարկիր migration (drizzle-kit
generate + push)։ Ստուգիր, թե subjectsTable-ը ինչ path-ով է import-վում
մնացած schema-ֆայլերում (օրինակ knowledge-topics.ts-ից), որ ուղիղ նույն
import-ը օգտագործես։

═══════════════════════════════════════════
ՄԱՍ 2 — Backend endpoint-ների ընդլայնում
═══════════════════════════════════════════

2.1. Գտիր (grep) այն backend route-ը, որ մշակում է class-ի ստեղծումն ու
թարմացումը (հավանաբար artifacts/api-server/src/routes/admin.ts, փնտրիր
"POST /admin/classes" կամ "createClass"-ի հետ կապված route)։ Ցույց տուր
ինձ գտած ֆայլն ու տողերը, նախքան փոփոխելը։

2.2. Ընդլայնիր POST /admin/classes route-ը (class-ի ստեղծում). ընդունիր
request body-ում լրացուցիչ դաշտ `subjectIds: number[]` (optional, կարող
է բացակայել)։ Class-ը insert անելուց անմիջապես հետո, եթե subjectIds
տրված է, insert արա համապատասխան տողերը teacherClassSubjectsTable-ում
(classId = նոր ստեղծված class-ի id, teacherId = req.body.teacherId,
subjectId = ամեն subjectIds-ից)։

2.3. Ընդլայնիր PUT/PATCH /admin/classes/:id route-ը (class-ի թարմացում)
նույն տրամաբանությամբ. եթե subjectIds տրված է, ջնջիր հին տողերը այս
classId-ի համար teacherClassSubjectsTable-ից և insert արա նորերը (replace,
ոչ append)։

2.4. Ավելացրու նոր GET endpoint. GET /admin/classes/:id/teacher-subjects
(կամ ավելացրու դաշտ արդեն գոյություն ունեցող GET /admin/classes response-ի
մեջ), որ վերադարձնի տվյալ class-ի համար արդեն ընտրված subjectId-երը՝ edit
ձևը կանխալրացնելու համար։

2.5. Ամենակարևորը. գտիր (grep) teacher-ի կողմից օգտագործվող endpoint-ը,
որ վերադարձնում է class-ի subjects-ը (հավանաբար GET /teacher/classes կամ
GET /teacher/classes/:id, կամ հենց schedule-ի endpoint-ը, որից բխում է
rawClassSubjects-ը teacher-dashboard.tsx-ում)։ Ընդլայնիր այն, որ
վերադարձնի նաև `assignedSubjects: string[]` (subject անունների ցուցակ)՝
հաշվարկված JOIN-ով teacherClassSubjectsTable + subjectsTable-ի միջոցով,
ֆիլտրված ըստ ընթացիկ ուսուցչի ID-ի և class-ի ID-ի։

═══════════════════════════════════════════
ՄԱՍ 3 — admin-dashboard.tsx-ի «Նոր Դասարան» ձևի ընդլայնում
═══════════════════════════════════════════

3.1. cForm state-ին ավելացրու` subjectIds: [] as number[]

3.2. Ուսուցչի <select>-ից անմիջապես հետո (Դասարան ստեղծելու ձևում),
ավելացրու նոր բլոկ, որ ցուցադրվի ՄԻԱՅՆ եթե cForm.teacherId ընտրված է.

  {cForm.teacherId && (() => {
    const selectedTeacher = teachers.find((t) => t.id === parseInt(cForm.teacherId));
    const teacherSubjects = selectedTeacher?.subjects ?? [];
    return (
      <div className="col-span-2">
        <label className="text-xs text-muted-foreground mb-1 block">
          Ինչ առարկա(ներ) կդասավանդի այս դասարանում
        </label>
        {teacherSubjects.length === 0 ? (
          <p className="text-xs text-amber-400">Այս ուսուցիչը որակավորության առարկա նշված չունի</p>
        ) : (
          <div className="grid grid-cols-2 gap-1.5">
            {teacherSubjects.map((subName) => {
              const subjectItem = subjectsList.find((s) => s.name === subName);
              if (!subjectItem) return null;
              return (
                <label key={subjectItem.id} className="flex items-center gap-2 text-sm cursor-pointer select-none rounded-lg px-3 py-2 border border-white/10 hover:border-primary/40 transition-colors">
                  <input
                    type="checkbox"
                    checked={cForm.subjectIds.includes(subjectItem.id)}
                    onChange={(e) =>
                      setCForm((f) => ({
                        ...f,
                        subjectIds: e.target.checked
                          ? [...f.subjectIds, subjectItem.id]
                          : f.subjectIds.filter((id) => id !== subjectItem.id),
                      }))
                    }
                    className="accent-indigo-500"
                  />
                  {subName}
                </label>
              );
            })}
          </div>
        )}
      </div>
    );
  })()}

3.3. handleCreateClass-ում, createClass.mutate-ի data object-ին ավելացրու
`subjectIds: cForm.subjectIds`: Reset-արա emptyClass-ը՝ ավելացնելով
`subjectIds: [] as number[]`:

3.4. Կրկնիր նույն 3.2-3.3 փոփոխությունը editClass-ի ձևի համար էլ
(խմբագրման ձև), նախապես լրացնելով subjectIds-ը GET /admin/classes/:id/
teacher-subjects-ից ստացած տվյալով, երբ editClass-ը բացվում է։

═══════════════════════════════════════════
ՄԱՍ 4 — teacher-dashboard.tsx-ի classSubjects-ի ուղղում
═══════════════════════════════════════════

Փոխարինիր ընթացիկ rawClassSubjects/classSubjects հաշվարկի տրամաբանությունը
(schedule-ի վրա հիմնված fallback-ը) ուղիղ նոր backend-ից եկող
`assignedSubjects` դաշտով (Մաս 2.5-ից)։ Եթե classDetail/schedule query-ն
արդեն վերադարձնում է այս դաշտը class-ի object-ի վրա, ուղղակի օգտագործիր
այն. `const classSubjects = selectedClass.assignedSubjects ?? []`:
Հեռացրու հին, schedule-ի վրա հիմնված fallback-ի ամբողջ logic-ը։

Ուշադրություն. եթե որևէ գոյություն ունեցող class/course արդեն ստեղծված է
առանց subjectIds-ի (հին տվյալ), assignedSubjects-ը դատարկ կլինի, ինչը
ճիշտ, ակնկալվող վարք է (admin-ը պիտի հետո ձեռքով նշանակի)։ Մի ավելացրու
լուռ fallback դեպի teacher.subjects, քանի որ դա հենց այն bug-ն է, որ
ուղղում ենք։

Ավարտից հետո ցույց տուր.
1. Backend route-ի գտած տեղը (2.1-ից) և փոփոխված կոդը
2. Ինչ ֆայլում/ինչ endpoint-ով է teacher-ը ստանում class-subjects (2.5)
3. TypeScript typecheck-ի արդյունքը (և´ frontend, և´ backend package-երի)
4. Migration-ի արդյունքը