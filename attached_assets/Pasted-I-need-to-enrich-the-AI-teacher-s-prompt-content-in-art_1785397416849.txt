I need to enrich the AI-teacher's prompt content in artifacts/api-server/src/routes/chat.ts so it actually uses our rich mapped lesson/node data, instead of the current generic hardcoded 4-phase text. Backend-only change (this file, plus reading columns that already exist — no schema changes needed).

MANDATORY ARMENIAN SCRIPT RULE: Any Armenian text you write or touch MUST be real Armenian Unicode script (Ա-Ֆ, ա-ֆ). Never transliterate Armenian into Latin letters or Cyrillic.

CONTEXT — what already exists that you should NOT change:
- The 4-phase structure itself (1=review, 2=new lesson basics, 3=deep dive + full check, 4=homework), currentPhase/currentNodeId progression, the mastery-gated advance-phase endpoint, evidence tracking, topic scoring, and the "###EVAL:CORRECT/INCORRECT/NONE###" inline evaluation tag mechanism all stay exactly as they are. Only the CONTENT of what gets sent to the model changes.
- `lessonsTable` already has `coreProblem: text`, `coreIdea: text`, and `practicalTasks: jsonb` (an array of objects, each shaped like `{ task: string, purpose?: string, sourcePage?: string | null, difficultyLevel?: "LOW"|"MEDIUM"|"HIGH", successCriteria?: string, relatedNodeTitle?: string, assignment?: "CLASS"|"HOMEWORK" }` — some older rows may be missing the newer fields, so treat all of these as optional/nullable in your code).
- `lessonNodesTable` already has `title`, `theoryContent`, `targetBloomLevel`, `estimatedMinutes`, plus these richer columns: `childFriendlyExplanation: text`, `basicExamples: jsonb` (string array), `realLifeExamples: jsonb` (string array), `commonMisconception: text`, `prerequisiteNodes: jsonb` (string array) — also all optional/nullable, may be empty on older rows.

CHANGE 1 — fetch the richer data
In the `POST /chat` handler, where the current node is currently fetched with only `{ title, theoryContent, targetBloomLevel, estimatedMinutes }`, also select `childFriendlyExplanation`, `basicExamples`, `realLifeExamples`, `commonMisconception`, `prerequisiteNodes`. Also read `lesson.coreProblem`, `lesson.coreIdea`, and `lesson.practicalTasks` from the already-fetched `lesson` row (no new query needed, `lesson` is already selected in full). Split `practicalTasks` into two arrays: `classTasks` = items where `assignment !== "HOMEWORK"` (treat missing/undefined assignment as CLASS, matching the existing backend default), and `homeworkTasks` = items where `assignment === "HOMEWORK"`.

CHANGE 2 — rewrite `buildPhaseInstruction`'s signature and content
Change its signature from `(phase, lessonTitle, subjectName)` to accept one options object with at least: `phase`, `lessonTitle`, `subjectName`, `coreProblem`, `coreIdea`, `node` (the richer current-node object or null), `classTasks`, `homeworkTasks`. Update the one call site accordingly. Keep the overall structure/tone/format rules already in each phase (Armenian-only, MCQ format, Unicode math notation, etc.) — you're enriching the CONTENT each phase instructs the model to teach with, not rewriting the pedagogical flow.

Phase 1 (review): unchanged in spirit (previous-lesson review questions), but if `coreIdea` is available, you may use it to help ground what "mastering this lesson" means for phrasing — keep this phase mostly as-is, it isn't the focus of this task.

Phase 2 (new lesson basics): instead of asking the model to invent an explanation of `lessonTitle` from scratch, instruct it to build the theory explanation from `node.childFriendlyExplanation` (if present, use it near-verbatim as the core explanation — this is a maintainer-approved child-friendly explanation, not something to reinvent) and `node.basicExamples` (present these as the illustrative examples). If `node.commonMisconception` is present, tell the model to design at least one MCQ distractor option based on that specific misconception, rather than inventing a generic wrong answer — this makes wrong-answer diagnosis meaningful.

Phase 3 (deep dive + full check): use `node.realLifeExamples` for the "deeper" real-world framing, and instruct the model to use `classTasks` as the actual exercises to give the student — present each task's `task` text as-is (don't invent new exercises when classTasks has entries), and when checking the student's answer, compare it against that task's `successCriteria` if present (told to the model as ground truth for grading, not shown to the student). If `classTasks` is empty for this lesson, fall back to the existing behavior of the model inventing exercises from theory content.

Phase 4 (homework): instead of asking the model to invent "3 tiers of homework from scratch", instruct it to present `homeworkTasks` (each task's `task` text, in a friendly closing message) as the actual homework — if `homeworkTasks` is empty, fall back to the existing 3-tier invented behavior so lessons without a CLASS/HOMEWORK split still work.

CHANGE 3 — update the `lessonContext` assembly
Where `lessonContext` is currently built (the array of LESSON/SUBJECT/nodeLine/DESCRIPTION/TEXTBOOK CONTENT lines joined together), add new lines when available: `CORE_PROBLEM: {coreProblem}`, `CORE_IDEA: {coreIdea}`, and pass the richer node fields into `buildPhaseInstruction` per Change 2 rather than inlining them all as raw context lines (the phase instruction is where they actually get used pedagogically). Keep `DUE_REVIEWS`, `TEXTBOOK CONTENT`, and everything else already there.

Do not change the schema, any other route, the frontend, or the eval-tag parsing logic. Run a typecheck and confirm zero errors, then show me: (a) the exact new `buildPhaseInstruction` call site, and (b) a sample Phase 2 instruction string it would generate for a node that has childFriendlyExplanation/basicExamples/commonMisconception populated, so I can review the actual prompt text before we test it live.