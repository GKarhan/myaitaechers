Read the full current content of these files first, paste back before changing anything: - artifacts/api-server/src/routes/quizzes.ts - artifacts/myaiteacher/src/pages/teacher-dashboard.tsx (the Թեստեր list block) - artifacts/myaiteacher/src/pages/dashboard.tsx (SectionHome's quiz block, and SectionQuizzes / "Իմ թեստերը") ## Step 1 — Backend: results data a) Extend GET /api/quizzes?subjectId=X (teacher) to also return, per quiz: completedCount (count of quiz_assignments with status = COMPLETED for this quizId), totalAssigned (total quiz_assignments for this quizId), and averageScorePercent (avg of quiz_attempts.scorePercent for this quizId's assignments, null if completedCount is 0). b) New endpoint GET /api/quizzes/:id/results (requireTeacher, verify quiz belongs to req.userId): return one row per assignment — student name, assignment status, and if completed: totalCorrect, totalQuestions, scorePercent, completedAt. Sort by scorePercent ASC (worst first, nulls/ not-completed last... actually put not-completed at the bottom regardless of sort, completed ones sorted worst-score-first above them). c) Extend GET /api/quizzes/assigned (student) to include, per assignment: status, and if COMPLETED: totalCorrect, totalQuestions, scorePercent. d) New endpoint GET /api/quizzes/:id/my-result (requireAuth, student): verify an assignment exists for (quizId, req.userId) with status COMPLETED, then return: totalCorrect, totalQuestions, scorePercent, and per-question detail — questionText, options, correctOptionIndex, the student's selectedOptionIndex, isCorrect (join quiz_answers + quiz_questions for this student's attempt). Paste back the full updated routes/quizzes.ts. ## Step 2 — Teacher UI: results in the Թեստեր list In teacher-dashboard.tsx's quiz row: next to the status badge, show "{completedCount}/{totalAssigned} ավարտել են" once totalAssigned > 0, and append " · միջին՝ {averageScorePercent}%" once completedCount > 0. Add an "Արդյունքներ" button next to "Դիտել" (only enabled/shown once completedCount > 0) that opens a modal or expands inline: fetch GET /api/quizzes/:id/results, render one row per student (name, score X/Y (Z%) or "Դեռ չի ավարտել" for incomplete ones), sorted worst-score-first as described, incomplete ones at the bottom. ## Step 3 — Student UI: results everywhere they see a quiz In dashboard.tsx, both SectionHome's quiz block and SectionQuizzes ("Իմ թեստերը"): for each assigned quiz where status === "COMPLETED", replace the "▶ Սկսել թեստը" button with "Տեսնել արդյունքը", and show inline under the title: "Ավարտված · {totalCorrect}/{totalQuestions} ({scorePercent}%)". For non-completed ones, keep exactly the current behavior. ## Step 4 — New result page (shared shape, two routes) Create artifacts/myaiteacher/src/pages/quiz-result.tsx and register two routes in App.tsx: /quiz/:id/result (student, calls GET /api/quizzes/:id/my-result) and reuse the same component for the teacher's per-student drill-down if useful, but the teacher's inline results list from Step 2 doesn't strictly need this page — only the student-facing one is required now. Page layout: header with overall score "X/Y ճիշտ (Z%)"; below, one card per question showing the question text, all 4 options, with the student's selected option highlighted (green if correct, red if incorrect) and the correct option marked with ✓ if the student got it wrong. No node-level analysis yet — that's a later phase. "Տեսնել արդյունքը" buttons from Step 3 navigate here. Paste back the full new file, the App.tsx route additions, and confirm typecheck passes across all changed files.